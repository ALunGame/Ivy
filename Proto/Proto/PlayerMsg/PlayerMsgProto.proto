syntax="proto2";

package PlayerMsg;
import "TypesProto.proto";

//指定命名空间（可选参数）如果没有，则使用C#代码的命名空间。
option csharp_namespace = "Proto";

message PlayerLevelChangeS2c {
	required player_info player = 1;
} 